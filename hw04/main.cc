#include <cstdio>

int main(int argc, char** argv){

	FILE* fp = fopen("test.ppm" , "rb");
	
	int array[];
	int width=0;
	int height=0;
	
	fread(array, sizeof(int), , fp);
	fwrite(aray, sizeof(int), , fp);
	
	const unsigned char* data[];

	
}