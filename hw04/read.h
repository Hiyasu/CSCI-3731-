#ifndef READ_H
#define READ_H
//I cannot get it to work with the header file. Keeps giving me undefined error during linking.
unsigned char* read(const char* file, char* header, int* width, int* height, int* numPixels);

#endif